// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyCsgyYeVBdMhF51efqo1C7Vk22HCHfqxyM',
    authDomain: 'teenagecoder-scholarship.firebaseapp.com',
    projectId: 'teenagecoder-scholarship',
    storageBucket: 'teenagecoder-scholarship.appspot.com',
    messagingSenderId: '295358684041',
    appId: '1:295358684041:web:fcd298ee905e308dd1df58'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
